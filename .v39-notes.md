# v39 scratch notes (delete before checkpoint)

User deployed the site (polyclone-n6b64njm.manus.space). v38 checkpoint f5b835d4 delivered.

## v39 scope (all 3 approved; road raiding is NOT in real Polytopia — user knows, wants it)
1. Road raiding: enemy unit on road tile severs trade node until cleared.
2. Trade-route glow: animated gold pulse along connected roads toward capital.
3. Hall of Conquest: persist per-match score breakdowns, show in history screen.

## Done so far
- rules.ts: connectedCityIds now skips road tiles occupied by non-friendly units (raided set); new helper `raidedRoadTiles(s, tribe)` returns occupied road tiles.
- rules.ts: `severedCityIds(s, tribe)` — cities connected on paper but cut by raiders (ghost-units trick).
- Hud.tsx: city panel shows rose "Trade route severed — raiders on the road!" badge (severedCityIds imported).
- state.ts: beginTurn logs severed routes for the human tribe next to the siege log; severedCityIds imported.

## NEW SCOPE (user request mid-task)
After v39 checkpoint: run a series of automated AI-vs-AI matches and report scores.
Plan: node script (tsx) importing core game modules headlessly (localStorage/window stubs like tests do), run N matches (basic vs pro AI, difficulties), collect winner, turns, final scores + scoreBreakdown per tribe; save CSV/JSON; analyze + report balance findings. Phases 6-7 in plan.

## Phase 1 DONE (road raiding)
- rules.ts: connectedCityIds skips raided roads; raidedRoadTiles + severedCityIds helpers.
- state.ts: beginTurn severed-route log (after siege log); imports updated.
- Hud.tsx: severed rose badge in city panel; severedCityIds imported.
- ai.ts + aiPro.ts: attack scorer bonus for hitting enemies on own-tribe road tiles (r.defenderDies ? 10/12 : 5/6), placed after colossus wall bonus. Type-check green.

## Landmarks for Phase 2 (trade pulse) + raided cue
- scene.ts road drawing: line ~664 in decorateTile-ish block, `if (t.road) {` — hub disc (CreateCylinder "road", mat "#c9a86a") + strips toward 4-adjacent road/city tiles. `top = h - 0.4`, position (t.x - c, top+0.025, t.y - c), c = this.center(s.size), parent this.root, decor.push(...).
- showHighlights(s) at scene.ts ~1778: disposes this.highlightMeshes then early-returns to showPlanner if plannerOpen && no unit selected. GameCanvas calls r.showHighlights(s) on every store 'changed' (GameCanvas.tsx ~27,38).
- TERRAIN_H[terrain] gives tile height h; rings sit at h - 0.4 + 0.08.
- Plan for pulse: a dedicated pulse layer — compute connected road path tiles for the human tribe (BFS from capital over road/city nodes honoring raiders), spawn small gold discs per road tile, animate via scene.registerBeforeRender sliding each disc toward the capital direction (or scale/alpha breathing). Rebuild pulse layer in buildBoard or showHighlights; store meshes in this.pulseMeshes + observer handle; dispose on rebuild. Raided cue: rose-tinted torus or recolor hub on raided road tiles (raidedRoadTiles imported into scene.ts).
- rules.ts exports needed in scene.ts: raidedRoadTiles, connectedCityIds (check existing import line in scene.ts: `from "../core/rules"` — has reachableTiles, attackableUnits, plannerSites, TERRAIN constants?).

## Phase 3 (Hall breakdowns) landmarks
- state.ts recordVictory ~1707: builds HallEntry {difficulty, faction, turns, score, mapSize, date}, loadHall/HALL_KEY "polyforge-hall", keeps best 5 sorted by turns then score. Add `breakdown?: ScoreParts` (import scoreBreakdown from rules) — compute at victory: scoreBreakdown(s, s.humanTribe).
- Menu.tsx hall rows ~653: `hall[hallTab]!.map((e: HallEntry, i) => ...)` — row shows faction, mapSize, T{turns}, score, date. Make rows expandable (useState expanded index) showing breakdown parts if e.breakdown exists. ScoreParts fields: cities, cityLevels, units, techs, battles, hero, heroFell, total.
- GameOver in Menu.tsx already has expandable breakdown UI (v38) — reuse label mapping: Cities ×100, City levels ×50, Army ×10, Techs ×40, Battles ×8, Hero, Hero fell.

## Phase 4 tests
- v39.features.test.ts: raiding severs connectivity + income (place enemy unit on road tile, expect connectedCityIds shrinks, starIncome drops by 1, severedCityIds contains city); raidedRoadTiles lists tile; AI-safe. Hall breakdown: recordVictory is private — test HallEntry persistence via winning state? Simpler: test scoreBreakdown fields + that loadHall parses entries with breakdown. Use localStorage stub pattern from v38 tests.

## Landmarks (from v38 work)
- rules.ts: connectedCityIds ~line 109; starIncome has trade section (+1★ per connected non-capital city); scoreBreakdown helper exists (cities/cityLevels/techs/army/battles/hero/heroFell) mirrors updateScore.
- state.ts: buildRoad action after buildPort (~line 1314); updateScore ~525-560; GameEvent union ~line 33; game store exported as `game`, emits {type:'changed'}.
- Hud.tsx: city panel shows Build road section + trade-network badge (Route icon); BottomBar has Planner toggle (Grid3x3).
- Menu.tsx: GameOver ~819+; standings rows expandable (scoreBreakdown, hint "Show score breakdown"); Hall of Conquest = history screen in Menu.tsx (search "Hall of Conquest" / match history persistence — need to find where finished matches are saved, likely localStorage).
- scene.ts: roads drawn in buildBoard tile decor (central paving disc + neighbor strips, search "road"); showPlanner method; wallCrushBurst/quakeFx FX; this.root parent; TILE size const; mat helper.
- GameCanvas.tsx: subscribes to 'changed', calls renderer refresh + showHighlights; combat/quake event FX handlers.
- Tests: server/v38.features.test.ts pattern (localStorage/setTimeout stubs, newGame, direct tile mutation). economy.v35.test.ts has building well-formed test.

## Remaining
- Raid feedback: log entry when route newly severed (endTurn check? compare connected set before/after enemy moves — simpler: at start of a tribe's turn, compute severed cities = cities with road-adjacent?? Keep simple: log at income time in endTurn when a city that WAS connected last turn is now severed; store lastConnected per tribe? Or skip log, do visual cue + badge). Red/amber cue on raided road tiles in scene.ts buildBoard or highlight layer; city panel badge "severed" state.
- AI: both brains gain small bonus for attacking units standing on own-tribe road tiles (defend trade); aiPro optionally parks on enemy roads (cheap: bonus in attack scoring like colossus-vs-walls pattern in ai.ts ~207-225, aiPro.ts ~295-310).
- Trade pulse: scene.ts — animated gold particles/discs moving along connected road path toward capital. Use registerBeforeRender or an animation on small discs; compute path via BFS parent map from capital (reuse connectedCityIds logic? need path tiles — write roadNetworkTiles helper returning connected road tiles + flow direction toward capital).
- Hall of Conquest: find history persistence (grep "hall\|history\|localStorage" in Menu.tsx / state.ts), add breakdown snapshot at gameover, render expandable in history.
- Tests: v39.features.test.ts — raiding severs connectivity/income, raidedRoadTiles, breakdown persisted in history record.
- Verify in browser, mark todos, rm this file, checkpoint, deliver with summary + 3 suggestions.
