# v37 implementation notes (scratch — delete before checkpoint)

Status: Phase 1 (Quake) code complete — types.quakeUsed, rules canQuake/quakeVictims/quakeWallTargets/QUAKE_DAMAGE=5, state.quake() + "quake" event, Hud Quake button (orange, Zap icon, spent hint), GameCanvas quake handler (quakeFx ring+shake, shatter/hitFlash victims, wallCrushBurst), both AI brains call store.quake. tsc clean. NOT yet in todo.md as [x] — todo update pending; tests pending phase 4.

## Code landmarks (verified)
- types.ts: UnitType "colossus" line ~107, UNIT_STATS.colossus line ~145. BUILDINGS array has sawmill/windmill with `adjacentTo` field and pop:0. Unit interface — need to add `quakeUsed?: boolean`.
- rules.ts: starIncome() at line 435 (structure above: per-city income, +workshop rewards). adjacencyPop(s,x,y,def) exists (8-way partner count). canBuild enforces one-per-city for defs with adjacentTo. knockbackDestination exported. WALL_DEFENSE_BONUS in types.ts.
- state.ts: attack() applies wall-crush (city.walls=false + event fields wallCrushed, knockback), moveUnit, build(x,y,id) with retro-growth for adjacency partners, nextUnit() line ~566. GameEvent union in state.ts lines ~25-45 — has "combat" with knockback/wallCrushed fields; add "quake" event {x,y,victims:[{id,x,y}], wallsBroken:[{x,y}]}.
- GameCanvas.tsx: consumes game events; has combat handler doing scene.slideUnit(...) for knockback + scene.wallCrushBurst(x,y). Add quake handler: camera shake + bursts.
- scene.ts: has slideUnit(unitId,from,to) hurl-slide and wallCrushBurst(x,y) masonry particles; starBurst pattern reusable. Buildings rendered in scene.ts ~lines 800-850 (sawmill/windmill meshes added there; add market mesh similarly).
- Hud.tsx: SelectionPanel (unit panel) — add Quake button when sel unit type==="colossus" && !quakeUsed. Build buttons show `+N` chips via adjacencyPop import; city panel lists buildings w/ (7,6)-style site buttons. BottomBar has onOpenTech/onOpenDiplo props; add planner toggle button there (Home.tsx wires BottomBar).
- ai.ts + aiPro.ts: both import adjacencyPop; attack scoring blocks ~ai.ts:207-225, aiPro.ts:295-310 have colossus-vs-walls bonuses. Add quake usage in unit action loop.
- Tutorial.tsx: STEPS ids incl. harvest/build/reward; ECON_DONE_KEY pattern for veterans.
- Tests: server/v36.features.test.ts has fresh() = game.newGame({size:11,seed:42,difficulty:"normal",preset:"pangaea",humanTribe:0}); put()/clearLand() helpers; previewCombat returns damageToDefender. vi.stubGlobal localStorage + setTimeout needed.
- economy.v35.test.ts: rewardChoicesForLevel in types.ts; POP_PER_LEVEL=3 in rules.

## Design decisions v37
- Quake: dmg 5 flat to all 8-adjacent enemy units (incl. guardians), breaks walls of any adjacent (or own-tile? use adjacent + same tile city) walled enemy city, sets attacked=true & moved=true, quakeUsed=true. Log entry + "quake" event.
- Market: BUILDINGS id "market", terrain grass, tech "sailing"? better: "riding"?? — pick existing tech list: organization, shields, hunting, archery, forestry, climbing, mining, smithery, riding, freespirit(?), chivalry, mathematics, sailing, navigation. Use "riding"→no; Polytopia markets need... choose "mathematics"? Simplest: reuse "mining"? Decide: tech "sailing" is odd; use "riding"?? — final: unlock with "free_spirit"? CHECK exact tech ids in types.ts before coding (grep TECHS). Market: starsPerAdjacent=1, partners: sawmill|windmill|workshop?? workshop is a city reward not a tile building — partners = sawmill/windmill only (tile-based), plus maybe farm? Keep: sawmill/windmill.
  NOTE: workshop is a city-level reward, not on tiles, so Market counts adjacent sawmill+windmill tiles. marketStars(s,x,y) helper; starIncome sums markets over owned tiles.
- Planner overlay: React overlay (not Babylon) too hard to position; better in-canvas? Simpler: PlannerPanel listing each city's best sites? User asked: dims map + overlays values on tiles. Implement via scene.ts: planner mode adds floating text sprites (DynamicTexture billboards) on buildable tiles + dark overlay plane; OR do DOM overlay using scene.project(tile)->screen coords. scene.ts likely has projection used by... check for `worldToScreen` or existing floating labels. Fallback: Babylon GUI TextBlocks.
- Prod deployed; user publishes manually. CI preference: user likes continuous checkpoints.

## Remaining sequence
1. Phase 1 Quake (types/state/rules? quake logic in state.ts; Hud button; GameCanvas+scene FX; ai both brains).
2. Phase 2 Market (types def, rules canBuild reuse adjacentTo one-per-city, starIncome, Hud chip shows +N★, scene mesh, AI).
3. Phase 3 planner overlay toggle in BottomBar.
4. Phase 4 tests server/v37.features.test.ts + pnpm check/test + browser verify.
5. Delete this file, checkpoint "v37", deliver with manus-webdev://<version>.
