{
  "name": "islandroadco-site",
  "version": "1.0.0",
  "private": true,
  "description": "Static site for islandroadco.com — Island Road Studios",
  "scripts": {
    "start": "serve public -c ../serve.json -l ${PORT:-8080}"
  },
  "dependencies": {
    "serve": "^14.2.4"
  }
}
