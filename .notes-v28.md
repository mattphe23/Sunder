# v28 Balance Fixes — status (2026-07-24)

## Context
User ran 4 playtests in the Playtest Lab; my cross-run analysis lives at
`docs/playtest-review-2026-07-24.md`. All 4 reports flagged snowballing.
User approved implementing all 5 proposed fixes.

## Implemented (all done, 69/69 tests green, tsc clean)
1. Anti-snowball package:
   - `rules.ts`: `coalitionStrikeBonus()` — pacted AI attackers deal +15% atk vs the
     runaway leader (commonEnemy). Wired in previewCombat + combatModifiers chip
     ("Coalition +15% vs leader").
   - `events.ts`: camps act every 1 turn (vs 2) when a leader exists; raid warbands
     up to 3 raiders; `worldUnitIntents` raiders see non-leader targets at 2× distance
     (bias) but act on true distance.
2. Riding baseCost 4 → 6 (`types.ts`).
3. Auren/scholars boost: start with 7 stars instead of 5 (`state.ts` newGame).
4. Ruin payout taper: `ruinTaper()` in state.ts — ×0.75 per prior ruinsClaimed,
   floor 40%, min 2 stars. Applied to small ruins + Great Ruins (star branches only).
5. Log clarity: camp messages now include (x, y) coords + leader-hunt callout;
   Research panel shows Scholars discount note (Hud.tsx).

## Tests
- New file `server/balance.v28.test.ts` (6 tests): riding cost, scholars stars
  (delta-based — tribe 0 gets first-turn income inside newGame), ruin taper math,
  coalition bonus gating, previewCombat increase, raider leader-bias.

## Next: re-run playtests on original 4 seeds (phase 2)
Original user runs (from playtest_runs DB table, ids 1-4):
- Check DB for exact params: `SELECT id, params FROM playtest_runs ORDER BY id` —
  runs used seeds/presets/tribes recorded there. Re-run with same params via
  `runPlaytest` (server/playtest.ts) using scripts/smoke-playtest.ts pattern
  (tsx + dotenv). Compare balance scores vs originals:
  originals: balance 4,5,5,6 (avg 5.0); clarity 7-8; fun 6-8.
- Then phase 3: checkpoint + deliver before/after comparison.

## Delivery reminders
- Original playtest review doc: docs/playtest-review-2026-07-24.md
- Checkpoint before delivering (last checkpoint 0993c7da predates balance fixes).
