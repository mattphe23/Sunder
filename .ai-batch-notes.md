# AI batch sim — working notes (scratch, delete before checkpoint)

## RESULTS (both batches complete, 160 games total)
Batch A (80g, roster [0,1,2,3], 4 difficulties × 2 presets × 10 seeds, size 11):
- Wins: Auren 51 (64%), Kharzul 18 (22%), Sunwei 9 (11%), Vessari 2 (2%) — CONFOUNDED with slot (Auren always slot 0)
- Paths: enlightenment 46%, score/dom 35%, bloodforge 12%, greatharvest 6%
- Turns: easy 23.8, normal 22.8, hard 18.4, impossible 20.6 (mean); cap-hits 9/80
- Winner breakdown means: techs 529, cityLevels 467, cities 400, battles 107, units 85
- Economy: winner income 14.2 / roads 1.6 / cities 4.0 vs losers 7.0 / 0.4 / 2.2

Batch B (80g, rotated rosters incl. Nerivane+Dravok, hard/continents, 10 seeds × 8 rosters):
- Win rate by DEF: Auren 46%, Sunwei 30%, Vessari 23%, Dravok 22%, Kharzul 20%, Nerivane 8%
- SLOT advantage: slot0 40%, slot1 29%, slot2 19%, slot3 12% — strong first-mover advantage
- Paths: score/dom 39%, enlightenment 24%, greatharvest 18%, bloodforge 10%, unbrokenwall 8%, plunderking 2%
- Avg scores: Dravok 1180, Vessari 1114, Kharzul 1074, Sunwei 1070, Nerivane 922, Auren 897
- Turns mean 21.9, cap-hits 13/80

## Key findings for report
1. Slot/turn-order advantage is the biggest imbalance (40% vs 12%) — bigger than any tribe passive
2. Auren strongest tribe even normalized (46%) — scholars+enlightenment path too efficient; also wins with LOWEST avg score (fast tech win)
3. Nerivane weakest (8%) — naval passive underperforms on continents; needs buff or map-dependent check
4. Enlightenment dominates victory paths at every difficulty (46% batch A)
5. Roads adoption low (1.6 winner avg) — AI paves little; trade network underused by AI
6. plunderking near-dead path (2%)
Files: /home/ubuntu/ai-sim/results.jsonl, results-rotated.jsonl, analyze.py, analyze_rotated.py

## Engine landmarks (verified)
- `runAiTurn(store, tribeIdx)` in core/ai.ts:33 — routes to `runProAiTurn` when s.difficulty === "impossible"
- `runProAiTurn(store, tribeIdx)` in core/aiPro.ts:140
- state.ts nextTribe() (line ~474): human turn ends → cycles tribes; AI turns run via setTimeout(150/60ms) → runAiTurn → endTurn
- s.turn >= s.maxTurns → endByScore(); maxTurns=30 typical
- aiBonus (state.ts ~424): easy=0, normal=1, hard=2, impossible=0 (income cheat stars per turn)
- newGame opts: { size, humanTribe, difficulty, seed, preset, humanTribes?, roster? }
- tribes have .isHuman, .alive, .score, .index; humanTribe drives isHuman
- scoreBreakdown(s, tribeIdx) from rules.ts returns {cities, cityLevels, units, techs, battles, hero, heroFell, total}

## Sim strategy
- Node script w/ tsx + localStorage & setTimeout stubs (copy from server/v39.features.test.ts):
  vi.stubGlobal not available outside vitest → plain `globalThis.localStorage = {...}`; setTimeout stub MUST run callbacks
  SYNCHRONOUSLY (or use real timers?) — NO: state.ts nextTribe uses setTimeout for AI turns; if stubbed as no-op the
  AI never moves. Approach: drive turns manually instead — set ALL tribes isHuman=false won't work (humanTribe required).
  Better: keep human tribe, but on each human turn call runAiTurn(game, humanTribe) manually, then game.endTurn();
  stub setTimeout to run callback IMMEDIATELY (fn => fn()) so AI turns chain synchronously.
  Guard: dismiss modals (showIntro=false, incomingOffer=null, perk choices auto-pick, recap dismissed).
- Perk choices: game auto-stages perkChoices for human hero level-ups? check `perkChoices`/`choosePerk` — auto-pick first.
- incomingOffer (peace) targets human → auto-respond via respondToOffer(false/true) or clear.
- Collect per game: seed, mapPreset, size, difficulty, winner, victoryType (domination if before maxTurns/winner set), turns,
  per-tribe final score + breakdown, income snapshot.
- Matrix: difficulties easy/normal/hard/impossible (basic brain w/ 0/1/2 cheat + pro brain), presets continents/pangaea,
  sizes 11; ~10 seeds each → ~80 games. Human-slot tribe also AI-driven (same brain as difficulty implies for fairness note:
  human slot uses runAiTurn(brain=difficulty routing) — on impossible the human slot also gets pro brain: symmetric).
- Output: JSON lines to /home/ubuntu/polytopia-clone/.sim-results/results.jsonl (gitignored? add to .gitignore or use /home/ubuntu/ai-sim/)
- Use /home/ubuntu/ai-sim/ OUTSIDE project to avoid deploy pollution; script sim.mts run with `npx tsx` from project root so imports resolve.

## Roster doc (user request, after balance report)
- Tribes in Menu.tsx/types.ts: Auren (scholars, blue), Kharzul (forge/attack, red), Sunwei (harvest, gold),
  Vessari (riders, purple), Nerivane (tide/naval, teal), Dravok (stone/walls, brown), Valkyra (storm, store), Mycelon (spore, store)
- Units in UNIT_STATS (types.ts): warrior, archer, rider(?), defender(?), swordsman(?), catapult/siege, boat variants, hero, colossus + tribe signature units (check types.ts unitTypes + custom tribe config)
- Deliverable: markdown doc, per tribe: identity/palette/lore + each unit with Polytopia-style visual prompt
