# v29 — Fix Polytopia's pain points (QoL + anti-turtling)

User direction (locked): stick to what people love about Polytopia + fix its known problems.
NO leader income taper (rejected). Use Polytopia-style levers.

## Code sites (surveyed)
- End Turn button: `client/src/game/ui/Hud.tsx` ~line 250 (BottomBar), calls `g.endTurn()`
- Unit flags: `u.moved`, `u.attacked` (types.ts 129-130); guardians have both true always
- AI turn delay: `setTimeout(..., 350)` in state.ts (2 sites: continueGame ~214, nextTribe ~470)
- Income: `starIncome(s, tribe)` in rules.ts line ~350 — capital 2 + city 1 + (level-1) each;
  granted in `beginTurn` state.ts ~359
- maxTurns: 30 default (state.ts ~1474 emptyState); endByScore at s.turn >= s.maxTurns
- Camera: BoardRenderer.camera (ArcRotateCamera), center offset = (size-1)/2; tile world pos =
  (x - c, 0, y - c). cameraGameSig recenter logic ~line 342.
- GameEvent union at state.ts line 28 — added `focusTile` event
- GameCanvas subscribes to game events; add focusTile handler → r.focusTile(s, x, y)

## Done so far
- [x] `focusTile` GameEvent + `BoardRenderer.focusTile()` (14-frame eased camera pan)
- [x] `game.unitsWithMoves()` (human units, not guardian, !moved || !attacked)
- [x] `game.nextUnit()` — cycles selection through unitsWithMoves, emits focusTile
- [x] GameCanvas focusTile handler → r.focusTile
- [x] Hud BottomBar: Next button (emerald, SkipForward icon, count badge) + Tab/N hotkey +
      end-turn inline nudge (first click warns "N units can still act — tap again to end",
      amber ring on button; nudge resets on turn change)
- [x] AI pacing: 350ms → 150ms in nextTribe (60ms when no human alive). NOTE: site at
      state.ts ~215 (continueGame) not yet changed — check if it matters.
- [x] Siege: starIncome — enemy unit (u.tribe >= 0) ON city tile → city contributes 0
- [x] Siege visibility: log line at owner's beginTurn + red SIEGE badge in TopBar (Hud.tsx)

## Remaining
- [ ] Check the other 350ms setTimeout at state.ts ~215 (continueGame path)
- [ ] AI aggression when ahead: in ai.ts scoring, if own force >> defender force nearby, reduce
      defensive camping weight (check ai.ts structure first)
- [ ] Tests: server/qol.v29.test.ts — unitsWithMoves excludes moved+attacked units & guardians;
      nextUnit cycles and wraps; siege income zeroing; AI delay unaffected in headless (setTimeout
      neutered in sim harness — check engine.sim.test.ts pattern)
- [ ] pnpm check + full pnpm test; visual verify Next button + siege badge via devgame screenshot
- [ ] Update todo.md v29 section as items complete; checkpoint; deliver

## Playtest lab rerun script pattern
- scripts/rerun-v28.ts replays seeds via runPlaytest; docs/v28-reruns/*.json outputs
- Original 4 user seeds: continents 841758 (Auren), archipelago 437396 (Auren),
  archipelago 657569 (Kharzul), continents 146213 (Kharzul)
